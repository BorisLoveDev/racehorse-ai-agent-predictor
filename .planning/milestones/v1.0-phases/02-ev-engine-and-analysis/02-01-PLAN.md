---
phase: 02-ev-engine-and-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/stake/analysis/__init__.py
  - services/stake/analysis/models.py
  - services/stake/pipeline/state.py
  - services/stake/settings.py
  - services/stake/pipeline/research/__init__.py
autonomous: true
requirements:
  - ANALYSIS-01
  - ANALYSIS-04
  - ANALYSIS-05
  - BET-02
  - BET-03
  - BET-04
  - BET-05
  - BET-06
  - SEARCH-02

must_haves:
  truths:
    - "All Phase 2 data contracts exist as importable Pydantic models"
    - "PipelineState has fields for research, analysis, sizing, and skip signals"
    - "SizingSettings configurable via STAKE_SIZING__ env vars"
    - "ResearchSettings has provider field configurable via STAKE_RESEARCH__PROVIDER"
  artifacts:
    - path: "services/stake/analysis/models.py"
      provides: "RunnerAnalysis, AnalysisResult, BetRecommendation, ResearchResult, ResearchOutput Pydantic models"
      exports: ["RunnerAnalysis", "AnalysisResult", "BetRecommendation", "ResearchResult", "ResearchOutput"]
    - path: "services/stake/pipeline/state.py"
      provides: "Extended PipelineState with Phase 2 fields"
      contains: "skip_signal"
    - path: "services/stake/settings.py"
      provides: "SizingSettings nested config"
      contains: "class SizingSettings"
  key_links:
    - from: "services/stake/analysis/models.py"
      to: "services/stake/pipeline/state.py"
      via: "models used as type annotations in state"
      pattern: "AnalysisResult|ResearchOutput"
---

<objective>
Define all Phase 2 data contracts, Pydantic models, settings extensions, and PipelineState fields before any implementation begins.

Purpose: Downstream plans (02, 03, 04) implement against these contracts. Interface-first ordering prevents scavenger hunts.
Output: Importable models, extended state, extended settings.
</objective>

<execution_context>
@/Users/borislovemac/projects/racehorse-agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/borislovemac/projects/racehorse-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ev-engine-and-analysis/02-CONTEXT.md
@.planning/phases/02-ev-engine-and-analysis/02-RESEARCH.md

<interfaces>
<!-- Existing contracts the executor must respect -->

From services/stake/parser/models.py:
```python
class RunnerInfo(BaseModel):
    number: int
    name: str
    barrier: Optional[int] = None
    weight: Optional[str] = None
    jockey: Optional[str] = None
    trainer: Optional[str] = None
    form_string: Optional[str] = None
    opening_odds: Optional[float] = None
    win_odds: Optional[float] = None
    win_odds_format: Optional[Literal["decimal", "fractional", "american"]] = None
    place_odds: Optional[float] = None
    place_odds_format: Optional[Literal["decimal", "fractional", "american"]] = None
    status: Literal["active", "scratched"] = "active"
    tags: Optional[List[str]] = None
    running_style: Optional[str] = None
    market_rank: Optional[int] = None
    tips_text: Optional[str] = None

class ParsedRace(BaseModel):
    # ... (track, race_number, runners, place_terms, etc.)
```

From services/stake/pipeline/state.py:
```python
class PipelineState(TypedDict, total=False):
    raw_input: str
    parsed_race: Optional[ParsedRace]
    detected_bankroll: Optional[float]
    active_runners: Optional[List[dict]]
    overround_raw: Optional[float]
    overround_active: Optional[float]
    enriched_runners: Optional[List[dict]]
    ambiguous_fields: Optional[List[str]]
    error: Optional[str]
```

From services/stake/settings.py:
```python
class ResearchSettings(BaseModel):
    model: str = "google/gemini-3.1-flash-lite-preview"
    temperature: float = 0.3
    max_tokens: int = 4000

class AnalysisSettings(BaseModel):
    model: str = "google/gemini-3.1-pro-preview"
    temperature: float = 0.7
    max_tokens: int = 8000

class StakeSettings(BaseSettings):
    parser: ParserSettings
    research: ResearchSettings
    analysis: AnalysisSettings
    redis: RedisSettings
    bankroll: BankrollSettings
    audit: AuditSettings
    # ... other fields
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 2 Pydantic models and analysis package</name>
  <files>
    services/stake/analysis/__init__.py,
    services/stake/analysis/models.py,
    services/stake/pipeline/research/__init__.py
  </files>
  <read_first>
    services/stake/parser/models.py,
    services/stake/settings.py,
    .planning/phases/02-ev-engine-and-analysis/02-RESEARCH.md
  </read_first>
  <action>
Create `services/stake/analysis/` package and `services/stake/pipeline/research/` package (empty `__init__.py` for both).

Create `services/stake/analysis/models.py` with these exact Pydantic models:

1. **ResearchResult** (per D-02, D-04, SEARCH-01):
   - `runner_name: str`
   - `data_quality: Literal["rich", "sparse", "none"]` — research completeness
   - `form_summary: str` — recent form
   - `trainer_stats: str` — trainer info
   - `expert_opinion: str` — tips/expert views
   - `external_odds: Optional[str]` — odds from external sources (ANALYSIS-05)
   - `confidence_notes: str` — data reliability notes

2. **ResearchOutput** (per D-02, D-03):
   - `runners: list[ResearchResult]`
   - `overall_notes: str` — general race context from research

3. **RunnerAnalysis** (per D-13, ANALYSIS-01):
   - `runner_name: str`
   - `runner_number: int`
   - `label: Literal["highest_win_probability", "best_value", "best_place_candidate", "no_bet"]`
   - `ai_win_prob: float` — AI-assigned win probability (0.0-1.0)
   - `ai_place_prob: Optional[float]` — AI-assigned place probability
   - `reasoning: str` — 2-3 sentence explanation (D-14)

4. **AnalysisResult** (per D-07, D-08):
   - `recommendations: list[RunnerAnalysis]`
   - `overall_skip: bool` — Tier 2 skip signal (D-07)
   - `skip_reason: Optional[str]` — why AI recommends skip
   - `ai_override: bool` — True if AI skips despite +EV (D-08)
   - `override_reason: Optional[str]`
   - `market_discrepancy_notes: list[str]` — external odds differences (D-15, ANALYSIS-05)

5. **BetRecommendation** (per D-13, BET-06):
   - `runner_name: str`
   - `runner_number: int`
   - `label: Literal["highest_win_probability", "best_value", "best_place_candidate", "no_bet"]`
   - `bet_type: Literal["win", "place", "skip"]`
   - `ev: float` — expected value
   - `kelly_pct: float` — Kelly fraction percentage
   - `usdt_amount: float` — exact USDT bet amount
   - `data_sparse: bool` — True if uncertainty discount applied (ANALYSIS-04)
   - `reasoning: str` — from analysis agent

All models import from `pydantic` (BaseModel, Field). Use `from typing import Literal, Optional`.
Export all models from `__init__.py`.
  </action>
  <verify>
    <automated>cd /Users/borislovemac/projects/racehorse-agent && source venv/bin/activate && python -c "from services.stake.analysis.models import ResearchResult, ResearchOutput, RunnerAnalysis, AnalysisResult, BetRecommendation; print('All models importable')"</automated>
  </verify>
  <acceptance_criteria>
    - services/stake/analysis/models.py contains `class ResearchResult(BaseModel)`
    - services/stake/analysis/models.py contains `class ResearchOutput(BaseModel)`
    - services/stake/analysis/models.py contains `class RunnerAnalysis(BaseModel)`
    - services/stake/analysis/models.py contains `class AnalysisResult(BaseModel)`
    - services/stake/analysis/models.py contains `class BetRecommendation(BaseModel)`
    - services/stake/analysis/models.py contains `label: Literal["highest_win_probability", "best_value", "best_place_candidate", "no_bet"]`
    - services/stake/analysis/models.py contains `data_quality: Literal["rich", "sparse", "none"]`
    - services/stake/analysis/models.py contains `ai_win_prob: float`
    - services/stake/analysis/models.py contains `ai_override: bool`
    - services/stake/analysis/__init__.py exists
    - services/stake/pipeline/research/__init__.py exists
  </acceptance_criteria>
  <done>All 5 Pydantic models importable from services.stake.analysis.models. Both packages created.</done>
</task>

<task type="auto">
  <name>Task 2: Extend PipelineState and Settings for Phase 2</name>
  <files>
    services/stake/pipeline/state.py,
    services/stake/settings.py
  </files>
  <read_first>
    services/stake/pipeline/state.py,
    services/stake/settings.py,
    services/stake/analysis/models.py,
    .planning/phases/02-ev-engine-and-analysis/02-CONTEXT.md
  </read_first>
  <action>
**Extend PipelineState** in `services/stake/pipeline/state.py`. Add these fields to the existing TypedDict (keep all existing fields intact):

```python
# Phase 2: Research, Analysis, Sizing fields
skip_signal: Optional[bool]           # True = skip this race
skip_reason: Optional[str]            # Why skipping
skip_tier: Optional[int]              # 1 = pre-analysis, 2 = post-analysis
research_results: Optional[dict]      # ResearchOutput.model_dump()
research_error: Optional[str]         # Error from research step
analysis_result: Optional[dict]       # AnalysisResult.model_dump()
final_bets: Optional[List[dict]]      # List of BetRecommendation dicts after portfolio caps
recommendation_text: Optional[str]    # Formatted recommendation for Telegram
```

Note: Use `dict` not Pydantic models in TypedDict — state goes through Redis FSM serialization. The Pydantic models define the schema; dicts carry the data.

**Add SizingSettings** to `services/stake/settings.py`. Create new nested BaseModel class (NOT BaseSettings — per established pattern):

```python
class SizingSettings(BaseModel):
    """Bet sizing configuration. Per BET-02 through BET-05."""
    kelly_multiplier: float = Field(
        default=0.25,
        description="Kelly fraction multiplier (0.25 = quarter-Kelly) per BET-02"
    )
    per_bet_cap_pct: float = Field(
        default=0.03,
        description="Max single bet as fraction of bankroll (3%) per BET-03"
    )
    max_total_exposure_pct: float = Field(
        default=0.05,
        description="Max total race exposure as fraction of bankroll (5%) per BET-04"
    )
    max_win_bets: int = Field(
        default=2,
        description="Max win bets per race per BET-04"
    )
    skip_overround_threshold: float = Field(
        default=15.0,
        description="Pre-analysis skip if margin exceeds this % per BET-05/D-06"
    )
    min_bet_usdt: float = Field(
        default=1.0,
        description="Minimum bet size in USDT"
    )
    sparsity_discount: float = Field(
        default=0.5,
        description="Sizing multiplier when research data is sparse per ANALYSIS-04/D-11"
    )
```

**Extend ResearchSettings** in `services/stake/settings.py`. Add `provider` field:

```python
class ResearchSettings(BaseModel):
    # ... keep existing fields ...
    provider: str = Field(
        default="online",
        description="Search provider: 'online' (OpenRouter) or 'searxng' per D-05/SEARCH-02"
    )
    searxng_url: str = Field(
        default="http://46.30.43.46:8888/search",
        description="SearXNG endpoint URL"
    )
```

**Add sizing to StakeSettings**:
```python
sizing: SizingSettings = Field(default_factory=SizingSettings)
```
  </action>
  <verify>
    <automated>cd /Users/borislovemac/projects/racehorse-agent && source venv/bin/activate && python -c "
from services.stake.pipeline.state import PipelineState
from services.stake.settings import get_stake_settings, SizingSettings
s = get_stake_settings()
print('skip_overround_threshold:', s.sizing.skip_overround_threshold)
print('kelly_multiplier:', s.sizing.kelly_multiplier)
print('provider:', s.research.provider)
print('searxng_url:', s.research.searxng_url)
# Verify PipelineState has new fields
import typing
hints = typing.get_type_hints(PipelineState)
assert 'skip_signal' in hints, 'missing skip_signal'
assert 'research_results' in hints, 'missing research_results'
assert 'analysis_result' in hints, 'missing analysis_result'
assert 'final_bets' in hints, 'missing final_bets'
assert 'recommendation_text' in hints, 'missing recommendation_text'
print('All Phase 2 state fields present')
"</automated>
  </verify>
  <acceptance_criteria>
    - services/stake/pipeline/state.py contains `skip_signal: Optional[bool]`
    - services/stake/pipeline/state.py contains `skip_tier: Optional[int]`
    - services/stake/pipeline/state.py contains `research_results: Optional[dict]`
    - services/stake/pipeline/state.py contains `analysis_result: Optional[dict]`
    - services/stake/pipeline/state.py contains `final_bets: Optional[List[dict]]`
    - services/stake/pipeline/state.py contains `recommendation_text: Optional[str]`
    - services/stake/settings.py contains `class SizingSettings(BaseModel)`
    - services/stake/settings.py contains `kelly_multiplier: float = Field(default=0.25`
    - services/stake/settings.py contains `skip_overround_threshold: float = Field(default=15.0`
    - services/stake/settings.py contains `provider: str = Field(default="online"`
    - services/stake/settings.py contains `searxng_url: str = Field(`
    - services/stake/settings.py contains `sizing: SizingSettings = Field(default_factory=SizingSettings)`
    - Existing Phase 1 fields in PipelineState unchanged (raw_input, parsed_race, etc.)
    - Existing Phase 1 settings classes unchanged (ParserSettings, RedisSettings, etc.)
  </acceptance_criteria>
  <done>PipelineState has all Phase 2 fields. SizingSettings configurable via env. ResearchSettings has provider and searxng_url fields. All existing Phase 1 contracts unchanged.</done>
</task>

</tasks>

<verification>
- All 5 Pydantic models importable from services.stake.analysis.models
- PipelineState TypedDict has 8 new Phase 2 fields alongside existing Phase 1 fields
- SizingSettings loadable with defaults (quarter-Kelly=0.25, per_bet_cap=0.03, max_exposure=0.05, skip_threshold=15.0)
- ResearchSettings.provider defaults to "online", configurable via STAKE_RESEARCH__PROVIDER
- Existing tests still pass: `PYTHONPATH=. pytest tests/stake/ -x -q`
</verification>

<success_criteria>
- `from services.stake.analysis.models import ResearchResult, ResearchOutput, RunnerAnalysis, AnalysisResult, BetRecommendation` succeeds
- `get_stake_settings().sizing.kelly_multiplier == 0.25`
- `get_stake_settings().sizing.skip_overround_threshold == 15.0`
- `get_stake_settings().research.provider == "online"`
- PipelineState type hints include skip_signal, research_results, analysis_result, final_bets, recommendation_text
- All existing Phase 1 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-ev-engine-and-analysis/02-01-SUMMARY.md`
</output>
